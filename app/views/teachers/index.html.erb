<main class="container">

    <div class ="col-sm-6 mx-auto">
        <%= form_with url: "/courses/search", data: {turbo_frame: "courses_result"}, method: :get  do |form | %>
            <%= form.search_field :query, class: "col-sm-6 mt-5" %>
            <%= form.submit "Search", class:"btn btn-primary" %>
        <% end %>
        <div class="row col-sm-8 mt-2">
        <%= link_to "New Course",new_course_path, data:{turbo_frame: "courses_result"},class: "btn btn-primary col-sm-4 ms-auto me-3"%>
        </div>
    </div>
  
    <%= turbo_frame_tag "courses_result" do%>
    
    <% end %>

</main>